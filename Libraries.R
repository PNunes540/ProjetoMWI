# Paulo Nunes
# Data: August 2017
# Library loading

library(R.matlab)
#library(data.table)
library(Cairo) #Confirmar: este ou ggplot2?
library(ggplot2) #Confirmar: este ou Cairo?
library(dplyr)

# library(reshape)

###library(RWeka)

library(wavelets)
library(e1071)
library(MASS)
#require(rpart)
#require(party)
#require(ipred)
#library(nnet)
#require(kohonen)
#library(randomForest)
#library(mda)
#library(DMwR)
#require(ff)

#library(FSelector)
#library(Matrix)

require(ROCR)
library(lattice)
library(caret)
memory.limit(4095)

library(randomForest)
require(party)

library(pracma)
